<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowContentTransitions">true</item>
        <item name="colorPrimary">@color/colorBackground</item>
        <item name="colorPrimaryDark">@color/colorBackground</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="PopupMenu" parent="Widget.AppCompat.PopupMenu.Overflow">
        <item name="android:background">@color/colorBackground2</item>
    </style>

    <style name="DialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:background">@color/colorBackground2</item>
        <item name="android:textColor">@color/colorNotActiveNavMenuItem</item>
        <item name="android:buttonStyle">@style/DialogButtonsTheme</item>
    </style>

    <style name="DialogButtonsTheme">
        <item name="android:textColor">@color/colorAccent</item>
    </style>

</resources>
